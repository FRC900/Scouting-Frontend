<%= form_for(@data_value) do |f| %>
  <% if @data_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_value.errors.count, "error") %> prohibited this data_value from being saved:</h2>

      <ul>
      <% @data_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_id %><br>
    <%= f.text_field :data_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
