<%= form_for(@match_category) do |f| %>
  <% if @match_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match_category.errors.count, "error") %> prohibited this match_category from being saved:</h2>

      <ul>
      <% @match_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :match_form %><br>
    <%= f.collection_select :match_form_id, MatchForm.order(:name), :id, :name, {prompt: "Select a Form"}, {}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
