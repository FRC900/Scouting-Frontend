<%= form_for(@scouter) do |f| %>
  <% if @scouter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scouter.errors.count, "error") %> prohibited this scouter from being saved:</h2>

      <ul>
      <% @scouter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
